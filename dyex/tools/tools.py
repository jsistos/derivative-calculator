#general dummy methods
def string_to_array(toArray):
    arr = []
    for char in toArray:
        arr.append(char)

    return arr

def array_to_string(toString):
    s = ''
    for char in toString:
        s.append(char)

    return s